package dev.prospectos.infrastructure.mcp.service;

/**
 * Deprecated placeholder kept only to avoid breaking references while the
 * runtime-backed metrics implementation replaces the original mock.
 */
@Deprecated(forRemoval = true)
public final class MockQueryMetricsService {

    private MockQueryMetricsService() {
    }
}